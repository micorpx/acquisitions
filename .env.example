# Server
NODE_ENV=development
PORT=3000

# Database
# For Docker development with Neon Local:
# - Set USE_NEON_LOCAL=true to use Neon Local container
# - DATABASE_URL is set in docker-compose.dev.yml (points to neon-local service)
# - Neon credentials (NEON_API_KEY and NEON_PROJECT_ID) must be set in .env

# Neon Local Configuration (REQUIRED for Docker development)
USE_NEON_LOCAL=true
NEON_API_KEY=your-neon-api-key-here
NEON_PROJECT_ID=your-project-id-here

# JWT
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Arcjet Security
ARCJET_KEY=your-arcjet-api-key

# CORS
CORS_ORIGIN=http://localhost:3000,https://yourdomain.com

# Logging
LOG_LEVEL=info
